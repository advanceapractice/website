name: Deploy Mobile Menu Fix to Hostinger

on:
  push:
    branches: ["claude/fix-layout-issues-JVbx3"]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    env:
      FTP_HOST: "89.117.139.62"
      FTP_USER: "u734158088"
      FTP_PASS: "rM4=WjlY&YSvbax;"

    steps:
      - uses: actions/checkout@v4

      - name: Upload aap-menu-fix.php to mu-plugins via FTP
        run: |
          curl --silent --show-error --ftp-create-dirs \
            --user "$FTP_USER:$FTP_PASS" \
            "ftp://$FTP_HOST/public_html/wp-content/mu-plugins/aap-menu-fix.php" \
            --upload-file aap-menu-fix.php
          echo "Upload complete"
